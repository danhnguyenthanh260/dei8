{
  "name": "dei8",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/www",
    "build": "npm run build --workspace=apps/www",
    "screenshot": "node scripts/screenshot.mjs"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
